<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <TextView
            android:id="@+id/projectsTitle"
            style="@style/SectionTitle"
            android:text="Our Projects"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/projectsDescription"
            style="@style/SectionContent"
            android:layout_marginTop="16dp"
            android:text="Explore our innovative waste management and recycling projects"
            app:layout_constraintTop_toBottomOf="@id/projectsTitle" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/projectsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toBottomOf="@id/projectsDescription" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView> 